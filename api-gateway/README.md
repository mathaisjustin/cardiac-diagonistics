# api-gateway

Spring Cloud Gateway — single entry point for the frontend, JWT validation, route-level
authorization, routing via Eureka. See
[`docs/00-infrastructure/api-gateway/README.md`](../docs/00-infrastructure/api-gateway/README.md)
once written, and [`docs/ARCHITECTURE.md`](../docs/ARCHITECTURE.md) for how this fits the whole
system.

🚧 Scaffold only — no code yet.
