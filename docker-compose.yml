# Skeleton only — placeholder for the deployment target described in
# docs/ARCHITECTURE.md and (once written) docs/04-deployment/docker-compose/README.md.
# Not runnable yet: no Dockerfiles, ports, envs, or health checks are filled in.
# Fleshed out once each service's own doc/code exists.

services:
  # --- built by us (one folder per service at repo root) ---
  frontend:
    build: ./frontend

  eureka-discovery:
    build: ./eureka-discovery

  api-gateway:
    build: ./api-gateway

  authentication-service:
    build: ./authentication-service

  user-profile-service:
    build: ./user-profile-service

  diagnosis-service:
    build: ./diagnosis-service

  bookmark-service:
    build: ./bookmark-service

  # --- off-the-shelf infra images, no custom folder needed ---
  kafka:
    image: TODO

  redis:
    image: TODO

  # one MySQL instance per owning service (authentication, user-profile, bookmark)
  # per docs/00-infrastructure/adr/0008-mysql-as-database-engine.md
  mysql:
    image: TODO
